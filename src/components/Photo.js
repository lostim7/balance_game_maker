function Photo() {
    const [photos, setPhotos] = useState([]);
    const getPhotos = async () => {
        const json = await (
            await fetch(
                ``
            )
        )
    }
}