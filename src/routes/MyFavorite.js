

function MyFavorites() {
  const favlist = ""
     if(favlist) {
        return <div>확인</div>
     } else {
        return <div>아직 저장한 이미지가 없습니다.</div>;
    }
    return
  }

export default MyFavorites;